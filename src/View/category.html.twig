{% extends 'layout.html.twig' %}
{% block title %} Category {% endblock %}
{% block content %}
    <section>
        <h1>Categorys</h1>
        <ul>
            {% for category in categorys %}
                <li>{{ category.name }}</li>
            {% endfor %}
        </ul>
    </section>

{% endblock %}
